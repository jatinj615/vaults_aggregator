import { useTheme } from '@mui/material';
import Image from 'next/image';

const LogoComponent = () => {
  const theme = useTheme();

  // if (theme.palette.mode === 'light') {
  //   return <Image src="" alt="scaling eth logo in light theme" height={40} width={195} />;
  // } else {
  //   return <Image src="" alt="scaling eth logo in dark theme" height={40} width={195} />;
  // }
  return <></>;
};

export default LogoComponent;
